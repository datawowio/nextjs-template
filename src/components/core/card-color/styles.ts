import { colors } from "@/config/palette";

export const styles = {
  colorPreview: {
    borderRadius: "10px 10px 0 0",
  },
  root: {
    border: "1px solid",
    borderColor: colors.other.primary.gray,
    borderRadius: "10px",
    width: "fit-content",
  },
};
