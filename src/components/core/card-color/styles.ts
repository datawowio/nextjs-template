import { colors } from "@/config/palette";

export const styles = {
  container: {
    width: "fit-content",
    border: "1px solid",
    borderColor: colors.other.primary.gray,
    borderRadius: "10px",
  },
};
